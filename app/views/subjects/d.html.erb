<% @page_title = "Delete #{@subject.name}" %>
<h1>Subjects#delete</h1>
<%= link_to("<< Back to List", subjects_path, :class => "back-link")%>
<div class="subjects delete">
<h2>Delete Subject</h2>
<%= form_for(@subject, :url => subject_path(@subject), :method => "delete") do |f| %>  <!--Must specific method-->

    <p>Are you sure you want to permanently delete this subject?</p>
    <p class="reference-name"><%= f.object.name %></p>   <!--same as @subject.name -->
    <div class="form-btn">
        <%= f.submit("Delete Subject")%>
    </div>
<%end%>
</div>
